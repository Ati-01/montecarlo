name: docs
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Package depends
  - numpy
  - matplotlib
  - math

    # Pip-only installs
  - pip:
        - sphinx-rtd-theme